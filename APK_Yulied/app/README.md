# TitanFit (app Flutter)

Código de la aplicación Android. El informe y la guía están en la raíz del proyecto (`../README.md`, `../INFORME.md`, `../GUIA_WINDOWS.md`).

```bat
cd app
flutter pub get
flutter build apk --release
```
